
INCLUDE BC68F3132.inc
;***************************************************************
;       MCU Interrupt Table
;***************************************************************
;-------------------------------------------------------------------------;				
;INT0 interrupt
;-------------------------------------------------------------------------;				

;-------------------------------------------------------------------------;				
;INT1 interrupt
;-------------------------------------------------------------------------;						
@vectort_int1i	.SECTION AT 0008H 'CODE'
		clr		hpeintf
		clr		hpeen
		reti
;-------------------------------------------------------------------------;				
;Time Base 0 interrupt
;-------------------------------------------------------------------------;	
extern	_FLAG1      :byte					
@vectort_tb0i 	.SECTION AT 000CH 'CODE'		
      	SET      _FLAG1.0 	  
		reti
;-------------------------------------------------------------------------;				
;Time Base 1 interrupt
;-------------------------------------------------------------------------;						
		
;-------------------------------------------------------------------------;
;Multi-function 0 Interrupt				
;-------------------------------------------------------------------------;		
extern RF_ISR :near				
@vectort_mf0i	.SECTION AT 0014H 'CODE'
   		JMP		RF_ISR
;-------------------------------------------------------------------------;				
;Multi-function 1 Interrupt
;-------------------------------------------------------------------------;				
@vectort_mf1i 	.SECTION AT 0018H 'CODE'
		clr		 CTM1AF	
		SET      _FLAG1.7 
		reti
		
		
;-------------------------------------------------------------------------;				
;Multi-function 1 Interrupt
;-------------------------------------------------------------------------;				
@vectort_lvdi 	.SECTION AT 001cH 'CODE'
	;	SET      _FLAG2.0 
		reti		
;-------------------------------------------------------------------------;				
;Multi-function 2 Interrupt
;-------------------------------------------------------------------------;						
;@vectort_mf2i 	.SECTION AT 001CH 'CODE'		
;		reti
;-------------------------------------------------------------------------;				
		end				
